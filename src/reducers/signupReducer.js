const initialState = {
  fullname: '',
  email: '',
  password: ''
}

export default function signupReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
