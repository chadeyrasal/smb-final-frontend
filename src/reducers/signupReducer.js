const initialState = {
  fullname: '',
  email: '',
  password: ''
}

export default function signupReducer(state = initialSate, action) {
  switch (action.type) {
    default:
      return state
  }
}
